# Handoff — Невариум CRM

Читать первым. Проект многосессионный: владелец не программист, между сессиями его ведёт
ИИ-агент. Всё, что не записано здесь или в `docs/DECISIONS.md`, следующая сессия не знает.

**Где что искать:** этот файл — текущее состояние и открытые пункты. `CLAUDE.md` — архитектура
и правила для агента. `docs/DECISIONS.md` — ADR-журнал (1–15) с обоснованиями. Подробная
история каждой вехи — в git log; здесь она сжата до сути намеренно, чтобы файл оставался
читаемым (до 2026-09-03 он разросся до 872 строк слоями и начал сам себе противоречить).

---

## 🧭 Где вы сейчас (карта на 2026-09-03)

**Работа лежит в двух worktree.** Это первое, что нужно знать: `git worktree list` покажет
четыре пути, и активная работа есть в двух из них.

| Worktree | Ветка | Что там | Состояние |
|---|---|---|---|
| `.claude/worktrees/nevarium-crm-continuation-57da67` | `claude/postgres-migration` | **Перевод на PostgreSQL** — основная работа | не закоммичено, 20 позиций в `git status` |
| `.claude/worktrees/intelligent-wu-2fe8b8` | `claude/handoff-app-js-review-0e84fe` | Три фикса багов в `server/app.js` | **закоммичено и запушено → PR #2, открыт** |
| `G:\nevarium-crm` (корень) | `main` | `5bc02f7`, смёржен PR #1 (логин-фикс) | чисто |
| `.claude/worktrees/terminal-8b68c5` | `claude/handoff-md-review-69d118` | Не наша, не трогали | ? |

**Начинать здесь:** `nevarium-crm-continuation-57da67`, ветка `claude/postgres-migration`.
Этот файл в актуальном виде лежит именно там. **Копия в корне репозитория (ветка `main`) —
устаревшая**: в ней нет ни ADR-009…015, ни перевода на Postgres. Если сессия открылась в
корне и вы читаете HANDOFF оттуда — вы читаете прошлое.

```bash
cd G:\nevarium-crm\.claude\worktrees\nevarium-crm-continuation-57da67
git status --short   # ожидается ровно 20 строк: 18 M + 2 ??
npm test             # ожидается 190 зелёных + 2 намеренно it.skip, всего 192
npm run build        # ожидается успех, фронт не менялся
```
Цифры разошлись — значит что-то менялось без этой записи; разбираться с этого.

**Правило проекта: не коммитить и не пушить без явной команды владельца.** На 2026-09-03
такая команда была ровно одна — по PR #2. Перевод на Postgres не коммитить.

---

## 🔴 Блокер №1: домен и TLS у `crm-nevarium.ru` (действие владельца, не код)

**Проверено живьём 2026-09-03** (и точно так же 2026-08-07 — за месяц не изменилось):

```
curl -sSI https://crm-nevarium.ru/api/leads
  → curl: (35) schannel: SEC_E_ILLEGAL_MESSAGE — TLS-рукопожатие падает
curl -sSI http://crm-nevarium.ru/api/leads
  → HTTP/1.1 404 Not Found, Server: openresty
```

DNS уже резолвится (`178.210.92.188`) и отвечает шлюз (`openresty` — похоже на Timeweb App
Platform), то есть DNS и, вероятно, само приложение настроены. **Не хватает привязки домена и сертификата в
панели Timeweb.** Пока TLS не работает, ни одна заявка с сайтов не доходит: браузер обрывается
на рукопожатии. Сделать может только владелец — у агента нет доступа к аккаунту.

**Проверка готовности:** `curl -sSI https://crm-nevarium.ru/api/leads` должен вернуть
HTTP-ответ (любой, хоть 404), а не TLS-ошибку.

Почему это блокер №1: сайт Лаб ИИ живой и его формы шлют заявки именно на этот адрес.
Значит **прямо сейчас заявки не собираются** — форма честно показывает ошибку и запасную
почту, но клиент, который не захотел писать письмо, потерян молча.

---

## 🔄 В работе: перевод CRM на PostgreSQL (не закоммичен)

**Зачем:** ADR-003 (SQLite на РФ-VPS) получил статус «заменено». Timeweb App Platform не
сохраняет файлы контейнера между деплоями — SQLite-файл терял бы все заявки при каждом
обновлении. План согласован владелицей отдельно: `nevarium-spb/nevarium-lab#3`,
ключевой документ — `docs/CRM_POSTGRES_MIGRATION_SPEC.md` в той ветке.

**Сделано целиком (этапы 1–6 плана):**
- `server/schema.sql` — одна цельная идемпотентная схема вместо массива миграций и
  `PRAGMA user_version`. Понятия «применить только недостающее» больше нет.
- `server/db-adapter.js` — асинхронный слой над `pg`, имитирующий интерфейс
  `.prepare(sql).get/all/run()` от better-sqlite3 (включая именованные `@col`-параметры),
  чтобы не переписывать текст SQL по всему `app.js`. Плюс `withTransaction`,
  `resyncIdentitySequence`, `tryClaimIdempotencyKey`.
- `server/app.js` — переведён целиком: auth, generic CRUD, дашборд, приём лидов/ПДн,
  анонимизация, экспорт/импорт, диагностика.
- **Идемпотентность публичного приёма переписана с check-then-act на claim-first.** Старая
  схема была безопасна только потому, что better-sqlite3 синхронный и между проверкой ключа
  и записью не было ни одного `await`. С `pg` это перестало быть верно.
- `telegram.js`, `bootstrap.js`, `backup.js`, `seed-admin.js`, `dev.js`, `index.js`.
  `backup.js` лишился `.sqlite`-копии: `VACUUM INTO` — SQLite-специфика без аналога в
  Postgres. Единственный формат бэкапа теперь JSON, его дополняют автобэкапы самой базы.
- `DB_FILE` → `DATABASE_URL` везде, включая `deploy/`.
- Тесты — на `pg-mem` (эмулятор в памяти) вместо `:memory:` SQLite.

**Проверено:** `npm test` — 190 зелёных + 2 намеренно `it.skip`, всего 192. `npm run build`
проходит. **Против настоящего Postgres не гонялось ни разу** — см. чек-лист ниже.

### Ревью перевода (gstack `/review`: 6 специалистов + адверсариальный проход)

Security и data-migration — чисто. Остальные нашли **5 настоящих дефектов, все исправлены**:

1. **Воронка на дашборде молча потеряла «(+N без суммы)».** Алиас `noAmount` был
   неквотированным, а Postgres складывает такие идентификаторы в нижний регистр → ключ
   приходил как `noamount`, фронт читает `f.noAmount` и получал `undefined`. SQLite регистр
   сохранял. Починено квотированием; проверено эмпирически.
2. **Сид проектов перестал быть атомарным.** Вставка шла по одному проекту без транзакции,
   а условие входа — «проектов ноль». Падение между двумя INSERT → при следующем старте
   проектов уже не ноль, сид не выполнится никогда, «Невариум Визор» пропадает навсегда,
   его заявки молча уезжают в проект по умолчанию. Обёрнуто в `withTransaction`.
3. **Воркер outbox мог ронять весь процесс CRM.** Внешний `SELECT` стоял в `try/finally`
   без `catch`, а `tick()` вызывается из `setInterval` — отказ базы давал необработанное
   отклонение промиса, что в Node по умолчанию валит процесс. С файлом SQLite это почти не
   случалось; с управляемым Postgres рестарт базы при обслуживании — штатное событие.
4. **`deploy/README.md` вёл владелицу по нерабочему пути:** ставил `DB_FILE` (код его больше
   не читает), **не содержал шага создания базы вообще** — то есть `DATABASE_URL` неоткуда
   было взять, а раздел восстановления предлагал скопировать в контейнер `.sqlite`-файл,
   которого `backup.js` больше не создаёт. Переписано; тот же разъезд починен в `README.md`.
5. Четыре комментария противоречили коду (ссылки на удалённые функции,
   `SQLITE_CONSTRAINT_UNIQUE`, неверное описание механизма захвата ключа).

Дополнительно: добавлены 2 теста на ветки claim-first, которых не покрывал ни один тест
(одновременные запросы с одним ключом; освобождение ключа после 429 — проверено, что тест
реально входит в retry-цикл, а не проходит вхолостую); добавлен составной индекс
`interactions(contact_id, happened_at)`; переотступлено тело `try` в `/api/leads`.

Сознательно **не** сделано: батчинг построчных INSERT в импорте дампа (оптимизировать путь,
обрабатывающий сегодня ноль строк, преждевременно); общий хелпер для claim/try/catch двух
публичных обработчиков (два вызова не оправдывают абстракцию); `schemaVersion` в
`/api/crm/diagnostics` остаётся захардкоженным `11` с комментарием.

### ⚠️ Обязательный ручной прогон на настоящем Postgres перед деплоем

Не перестраховка: каждый пункт — дыра, которую `pg-mem` закрыть не может в принципе.

- **Атомарность отката импорта** — два `it.skip`-теста (битый файл, чужеродная колонка).
  После отклонённого импорта старые данные обязаны остаться на месте. `pg-mem` не даёт
  реальной изоляции: вставка внутри откаченной транзакции у него остаётся закоммиченной.
- **`resyncIdentitySequence`** — под `pg-mem` пишет в лог «пропущено —
  pg_get_serial_sequence недоступна» (5 мест) и **никогда не выполняет свой `setval`**. Под
  настоящим Postgres этих строк быть не должно. Проверка по существу: импортировать дамп,
  затем создать новый контакт руками — оба должны сосуществовать без ошибки PRIMARY KEY.
- **`pg.types.setTypeParser(20, parseInt)`** (`server/db.js`) — лечит BIGINT-как-строку от
  `COUNT()`/`SUM()`. `pg-mem` отдаёт числа нативно, поэтому тесты зелены и **без** этой
  строки: регрессия здесь автотестами не ловится вообще. Проверка: открыть дашборд и
  убедиться, что счётчики и суммы воронки — числа, а не строки.
- **Импорт дампа с воронкой возврата** — FK-порядок `tasks → winback_sequences → deals`.

### Ограничения `pg-mem`, не относящиеся к реальному Postgres

Держать в голове: это движок тестов, а не СУБД. Всё найдено изолированной репродукцией;
где обходной путь валиден и для настоящего Postgres — применён в коде.

1. `BEGIN/COMMIT/ROLLBACK` парсятся, но не дают реальной изоляции (см. чек-лист выше).
2. `INSERT ... ON CONFLICT DO NOTHING RETURNING` при конфликте возвращает существующую
   строку вместо пустого набора — поэтому захват ключа сделан через простой `INSERT` +
   `catch` на `23505`.
3. `pg_get_serial_sequence()` не реализована вовсе.
4. Повторный `CREATE TABLE IF NOT EXISTS` на уже применённой схеме падает внутренней
   ошибкой — не мешает, т.к. каждый тест поднимает свой инстанс.
5. `stage NOT IN (...)` на индексированной колонке падает внутри `BIndex.nin`. Обход:
   `NOT (stage IN (...))` — семантически то же самое.
6. Коррелированный `NOT EXISTS`/скалярный подзапрос вместе с `GROUP BY` по колонке из
   JOIN даёт ложное «column "c.id" does not exist». Обход: агрегация вынесена в
   некоррелированную производную таблицу.
7. Возвращает `COUNT`/`SUM` числами, а не строками, маскируя необходимость `setTypeParser`.

### Открытые пункты по переводу

1. **Ручной прогон на настоящем Postgres** (чек-лист выше) — нужен `DATABASE_URL` от
   владелицы, Этап 4 плана переезда.
2. **`deploy/docker-compose.yml`** — открытый архитектурный вопрос, не решён молча: Вариант Б
   (самостоятельный VPS) без своего сервиса `postgres:` или внешнего `DATABASE_URL` рабочую
   базу не поднимет. Нужно решить: добавлять `postgres:` в compose или отказаться от
   Варианта Б в пользу управляемой базы даже там.
3. **Кросс-модельная проверка (Codex) не выполнена — блокер среды.** `codex` установлен и
   авторизован, но аккаунт отвергает все модели: «The '<model>' model is not supported when
   using Codex with a ChatGPT account» (пробовал `gpt-5.6-sol`, `gpt-5-codex`,
   `gpt-5.1-codex-max`), плюс повреждён кэш моделей. Это про установку Codex, не про этот
   дифф. Второго мнения от другой модели по переводу нет.
4. `docs/DECISIONS.md` не трогал: решение по Postgres зафиксировано в связанном репозитории
   (`nevarium-lab#3`). Если нужен свой ADR-016 здесь — это отдельное решение.

---

## ✅ PR #2 — три фикса из ревью `server/app.js` (открыт, ждёт ревью)

https://github.com/nevarium-spb/nevarium-crm/pull/2 — ветка
`claude/handoff-app-js-review-0e84fe` → `main`, коммит `9d39471`, 144/144 теста зелёные.
Все три бага подтверждены воспроизведением, а не рассуждением:

1. Битый заголовок `Origin` ронял мутации `/api/crm/*` в **500 вместо 403**
   (`new URL(origin)` без `try/catch`). Актуально, пока не задан `APP_ORIGIN`.
2. `closed_at` затирался при PATCH сделки той же стадией: форма редактирования всегда шлёт
   текущую стадию, поэтому правка заметки у закрытой сделки теряла настоящую дату закрытия.
3. `PATCH`/`DELETE /api/crm/users/:id` без проверки id отвечали `{ok:true}` и писали в
   журнал, ничего не изменив.

**⚠️ При слиянии:** ветка `claude/postgres-migration` растёт от
`claude/vizor-crm-requirements`, а не от `main`, поэтому **этих трёх фиксов в ней нет** —
проверено, соответствующие строки там в исходном виде. После слияния PR #2 фиксы нужно
перенести в ветку миграции или аккуратно разрешить конфликт, иначе перевод на Postgres
вернёт все три бага обратно.

---

## 📋 Открытые пункты

### Действия владельца (не код)

1. **P0: привязать домен и выпустить сертификат** в панели Timeweb (см. блокер №1 выше).
2. **Создать Managed PostgreSQL** в Timeweb, включить автобэкапы, передать `DATABASE_URL`.
   Шаг добавлен в `deploy/README.md`; регион — РФ (152-ФЗ), как и приложение.
3. **Telegram-бот** — токен и chat_id; статус не проверялся с июля.
4. **Аккаунт Timeweb** dv395710 (ИП Макеева Марина) — на 2026-07-28 баланс был 0 ₽. Раз
   App Platform отвечает, значит пополнен; актуальный статус не проверял.
5. После первого деплоя — снять `BOOTSTRAP_ADMIN_*` из настроек, войти в `/crm`.
6. **Уведомление в РКН** — бумажный шаг. Черновик `docs/rkn-notification-draft.md` готов,
   но устарел (см. расхождения ниже).

### Код

1. **Форма запроса ПДн на сайтах** — серверный `POST /api/pd-requests` готов и проверен,
   форм на `G:\nevarium1` и `G:\nevarium_vizor` нет. Пока запросы приходят на почту и
   заводятся вручную.
2. **Слияние уже существующих дублей** — новые не появляются (ADR-014), но слить
   заведённые вручную пока нечем. До деплоя базы нет, так что не срочно.

---

## ⚠️ Расхождения, которые надо развести до запуска

1. **Политика ПДн обещает больше, чем делает код.** Опубликованный текст говорит про
   обезличенные уведомления в мессенджер, а `leadMessageFull()` после ADR-009 шлёт в MAX
   имя, контакт и задачу. В Telegram по-прежнему уходит обезличенное — расхождение именно
   с MAX. Пока CRM не запущена, обещание никого не вводит в заблуждение; **в момент
   запуска оно становится ложным**. Привести в соответствие до старта: либо текст политики,
   либо содержимое уведомления.
2. **Черновик уведомления в РКН устарел** — единственный коммит от 28.07, до ADR-009…015
   (MAX как второй канал, права субъекта ПДн, слепок согласия, перевод на Postgres).
   Перечитать перед подачей.
3. **Сайт Лаб ИИ уже запушен и живёт** (`www.nevarium-lab.ru`, мигрирован на Astro) — это
   проверялось 2026-09-01 через `git ls-remote`, сегодня не перепроверял. Старый порядок
   «сначала деплой CRM, потом пуш сайтов» **нарушен**: сайт живой, а CRM нет, поэтому
   заявки не собираются. Про сайт Визора статус пуша не проверял.

---

## 📚 Что уже сделано (сжато; подробности — в git log и `docs/DECISIONS.md`)

- **Вехи 1–5** — извлечение в отдельный репозиторий, модель `project_id`, приём заявок с
  привязкой к проекту, фильтр в списках, дашборд и аналитика по проектам. ADR-001…006.
- **Веха 7** — воронка возврата после отказа: перевод сделки в «Проиграно» заводит серию из
  3 задач (7/30/60 дней), возврат в работу снимает незакрытые. Без писем — почты нет.
  ADR-007. Если появится SMTP, расписание вынесено в `WINBACK_STEPS`.
- **Деплой одним контейнером** — Node сам отдаёт и API, и собранный фронт; первый админ
  через `BOOTSTRAP_ADMIN_*` (на App Platform нет shell). ADR-008.
- **MAX как второй канал и разделение ПДн** — в Telegram никогда ничего с ПДн (зарубежный,
  трансграничная передача), в MAX можно (российский). Ночной бэкап уходит только в MAX.
  ADR-009. Требует корневой сертификат Минцифры (`server/certs/`).
- **Права субъекта ПДн** — `/crm/privacy`, `pd_requests`, `audit_log`, обезличивание вместо
  удаления (сделка со стадией и суммой остаётся, чтобы не поехала воронка). ADR-011.
- **Блок «Остывают»** на дашборде, **экспорт/импорт JSON как путь восстановления** (ADR-013),
  **дедупликация контактов на приёме** (ADR-014).
- **ТЗ сайта Визор, P1** — идемпотентность, жёсткий rate-limit, honeypot, статус
  `pending_unverified` с отдельной колонкой `verified_at`, слепок согласия (`consents`,
  152-ФЗ ст.9 с 2026-09-01), квота уведомлений на проект. ADR-015. Ревью заняло 28 раундов
  `/codex:adversarial-review`; разбор каждого — в ADR-015.

---

## Как запустить

```bash
npm install
# DATABASE_URL обязателен: файла-базы «из коробки», как было у SQLite, больше нет
DATABASE_URL=postgresql://user:pass@localhost:5432/nevarium_crm node server/dev.js  # :3001
npm run dev     # :5173 → открыть / (перекинет на /crm)
npm test        # Postgres НЕ нужен: тесты поднимают pg-mem на каждый тест
```
Первый админ: `npm run seed-admin` (интерактивно) или `BOOTSTRAP_ADMIN_*` при первом старте.

## Правила

- **Не коммитить и не пушить без явной просьбы.** Владелец решает, когда «отгружаем».
- **Не менять поведение без согласования** — сначала показать, потом менять.
- `G:\nevarium` не трогать — только источник, откуда извлечена CRM.
- Хранение ПДн — только РФ (152-ФЗ), включая управляемый Postgres.
