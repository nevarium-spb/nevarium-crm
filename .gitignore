node_modules
dist
data/*.sqlite
data/*.sqlite-*
.env
*.log

# Локальные настройки среды агента (права на команды и т.п.) — у каждой машины свои.
# Именно этот файл, а не вся .claude/: общие настройки проекта коммитить можно.
.claude/settings.local.json
